services:
    php81:
        build:
            context: .
            dockerfile: Dockerfile
            target: php81
        container_name: 68publishers.image-storage.81
        volumes:
            - .:/var/www/html:cached

    php82:
        build:
            context: .
            dockerfile: Dockerfile
            target: php82
        container_name: 68publishers.image-storage.82
        volumes:
            - .:/var/www/html:cached

    php83:
        build:
            context: .
            dockerfile: Dockerfile
            target: php83
        container_name: 68publishers.image-storage.83
        volumes:
            - .:/var/www/html:cached
